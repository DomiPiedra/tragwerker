/** Dispatched after "/" → Image removes the trigger so the editor can open the media picker. */
export const EDITOR_OPEN_MEDIA_PICKER_EVENT = "hcms:editor-open-media-picker";

/** Dispatched after "/" → Video to open the dedicated video picker sheet. */
export const EDITOR_OPEN_VIDEO_PICKER_EVENT = "hcms:editor-open-video-picker";
